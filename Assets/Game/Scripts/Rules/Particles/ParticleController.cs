using UnityEngine;

namespace Supinflow
{
    /// <summary>
    /// Comportement d'une particule colorée individuelle.
    /// TODO: gérer la couleur de la particule et son rendu (SpriteRenderer).
    /// TODO: réagir aux collisions (traits, obstacles, contenants) via Rigidbody2D.
    /// TODO: se détruire si la particule sort de l'écran.
    /// </summary>
    public class ParticleController : MonoBehaviour
    {
        // TODO: propriété Color + méthode SetColor(Color).
    }
}
