using UnityEngine;

namespace Supinflow
{
    /// <summary>
    /// Sauvegarde et chargement de la progression du joueur.
    /// TODO: persister niveaux débloqués, étoiles obtenues, réglages audio.
    /// TODO: choisir le support (PlayerPrefs ou JSON dans persistentDataPath).
    /// </summary>
    public class SaveSystem : MonoBehaviour
    {
        // TODO: méthodes Save(), Load(), ResetProgress().
    }
}
