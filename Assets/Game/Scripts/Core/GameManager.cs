using UnityEngine;

namespace Supinflow
{
    /// <summary>
    /// Gestionnaire central du jeu (singleton).
    /// TODO: gérer l'état global (menu, en jeu, pause, victoire, défaite).
    /// TODO: orchestrer les transitions entre scènes et les autres managers.
    /// </summary>
    public class GameManager : MonoBehaviour
    {
        // TODO: instance singleton persistante (DontDestroyOnLoad).
        // TODO: machine à états du jeu + événements (OnLevelWon, OnLevelLost...).
    }
}
